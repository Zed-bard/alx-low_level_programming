/* This program prints the string "Programming is like building a multilingual puzzle"
   to standard output. */

#include <stdio.h>

int main() {
  /* The `puts` function is used to print a string to standard output. */
  puts("Programming is like building a multilingual puzzle");

  /* The `return 0` statement is used to indicate that the program terminated successfully. */
  return 0;
}
